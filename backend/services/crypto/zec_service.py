
def enviar_zec(from_address, to_address, amount, zec_node_url, private_key):
    # Esta função deve ser implementada com integração real com zcashd ou carteira via RPC
    # Aqui apenas simula
    return f"simulated_tx_zec_{from_address[:6]}_{to_address[:6]}"
